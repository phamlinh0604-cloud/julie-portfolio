---
// Hero component for homepage
---

<section class="hero-editorial">
  <div class="container-editorial">
    <div class="hero-content-editorial">
      <h1 class="hero-intro">
        Hi, I'm <span class="underline-accent">Julie Pham</span> and I am a fintech professional.
      </h1>
      
      <p class="hero-body">
        I've led cross-functional teams to build <span class="underline-accent">shop-now-pay-later</span> products for small business owners and <span class="underline-accent">cash loan</span> products for drivers and small business owners from 0 to 1.
      </p>
      
      <p class="hero-closing">
        I'm passionate about how technology, data, and AI can transform digital products.
      </p>
    </div>
  </div>
</section>

<style>
  /* Editorial Minimalism - Hero Section */
  .hero-editorial {
    background: #ffffff;
    padding: 120px 0 100px;
    min-height: auto;
  }
  
  .container-editorial {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 0px;
  }
  
  .hero-content-editorial {
    text-align: left;
    animation: fadeInUp 0.8s ease-out;
  }

  @keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
  }
  
  /* Main Headline - Large Serif */
  .hero-intro {
    font-family: 'Cormorant Garamond', 'Cochin', Georgia, serif;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 200;
    line-height: 1.4;
    color: #1a1a1a;
    margin: 0 0 2.5rem 0;
    letter-spacing: -0.03em;
  }
  
  /* Body Paragraphs - Serif with generous spacing */
  .hero-body {
    font-family: 'Cormorant Garamond', 'Cochin', Georgia, serif;
    font-size: clamp(1.25rem, 2.5vw, 1.75rem);
    font-weight: 400;
    line-height: 1.8;
    color: #1a1a1a;
    margin: 0 0 2rem 0;
    letter-spacing: -0.01em;
  }
  
  /* Closing Statement */
  .hero-closing {
    font-family: 'Cormorant Garamond', 'Cochin', Georgia, serif;
    font-size: clamp(1.125rem, 2vw, 1.5rem);
    font-weight: 400;
    line-height: 1.7;
    color: #1a1a1a;
    margin: 3rem 0 0 0;
    letter-spacing: -0.01em;
  }
  
  /* Underline Emphasis (Like Nicolas Backal) */
  .underline-accent {
    text-decoration: underline;
    text-decoration-color: #1a1a1a;
    text-decoration-thickness: 1px;
    text-underline-offset: 3px;
    font-weight: 400;
    transition: text-decoration-color 0.2s ease;
  }
  
  .underline-accent:hover {
    text-decoration-color: #ffffff;
  }
  
  /* Responsive adjustments */
  @media (max-width: 768px) {
    .hero-editorial {
      padding: 60px 0 40px;
    }
    
    .container-editorial {
      padding: 0 20px;
    }
    
    .hero-intro {
      font-size: 2rem;  /* Smaller on mobile */
      line-height: 1.3;
      margin-bottom: 1.5rem;
    }
    
    .hero-body {
      font-size: 1.125rem;
      line-height: 1.6;
      margin-bottom: 1.25rem;
    }

    .hero-closing {
      font-size: 1rem;
      margin-top: 2rem;
    }
  }
</style>